websockets>=12.0,<14.0
# opencv and numpy are installed via apt (python3-opencv, python3-numpy)
# picamera2 is pre-installed on Raspberry Pi OS Bookworm
# All accessed via --system-site-packages venv
# picamera2 is pre-installed on Raspberry Pi OS Bookworm — do NOT pip install it
# If testing on a laptop, comment out the picamera2 import in camera.py
